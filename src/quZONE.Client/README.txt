Testing gulp build tool

To build the project:

1. install all npm packages by running npm install, if not installed;
2. run gulp clean;
3. run gulp build;
4. run gulp minifyJs;
5. change the main.js to main.min.js (make suer main.min.js has been successfully generated in the folder of js);
6. test.
