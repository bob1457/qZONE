Branches:

1. master - main production version to be tested and deployed
2. develop - new features are being developed  - current initial version, without billing operations
3. staging(local name) dev-v1(remote name - github) 


Merge operattions:

develop merges to staging(remote: stating)
staging merges to master

After merge, then push to remote.